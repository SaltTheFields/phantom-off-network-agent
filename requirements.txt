# LLM backend — Ollama Python client
ollama>=0.3.0

# Web search — no API key, no account required
ddgs>=0.1.0

# Web content extraction
trafilatura>=1.8.0
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=5.0.0

# RSS feed monitoring (stdlib xml fallback also available)
# no extra deps required — uses requests + regex parser

# Semantic memory (optional — falls back to keyword search if not installed)
# pip install sentence-transformers
# sentence-transformers>=2.7.0

# Web UI (optional — only needed if using web_app.py)
# pip install fastapi "uvicorn[standard]"
# fastapi>=0.111.0
# uvicorn[standard]>=0.29.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
